_format_version: "3.0"

plugins:
  - name: proxy-cache
    config:
      strategy: memory
      cache_ttl: 300               # Keeps cache responses alive for 5 minutes
      content_type:
        - application/json; charset=utf-8
      response_code:
        - 200                      # Protects memory by only caching successful 200 OK responses